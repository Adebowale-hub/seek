# SEEK Map App Environment Configuration (Optional)
# Out-of-the-box, SEEK uses OpenStreetMap + CartoDB tiles + OSRM routing with zero configuration required.
# To upgrade map tiles and routing to Mapbox Vector/Tiles, supply your Mapbox Access Token below:

VITE_MAPBOX_TOKEN=your_mapbox_access_token_here
